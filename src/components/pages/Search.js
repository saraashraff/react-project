import React from 'react';
import '../../App.css';
import Results from '../Results';
import SearchArtist from '../SearchArtist';

function Search(){
    return (
        //search page which displays the artists search results.
        <div>
        <Results/>
        </div>
    );
}
export default Search;
